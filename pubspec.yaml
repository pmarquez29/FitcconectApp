name: app_fitconnect
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  socket_io_client: ^3.1.2
  http: ^1.1.0
  fl_chart: ^0.68.0
  percent_indicator: ^4.2.3
  flutter_riverpod: ^2.4.0
  shared_preferences: ^2.5.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/logo.png
    - assets/rutina_placeholder.png

